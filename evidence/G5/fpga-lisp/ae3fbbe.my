; Data only, per evidence/README.md's schema (my-lisp, f96936d). Read via
; (read-file ...), never (load ...).

(evidence
  (fixture . "(reverse '(a b c))")
  (requirement . G5)
  (implementation . fpga-lisp)
  (commit . "ae3fbbe")
  (runner . iverilog)
  (expected . "(c b a)")
  (actual . "(c b a)")
  (result . pass)
  (timestamp . "2026-08-11")
  (note . "Second mutually-recursive letrec pair (reverse-onto/reverse) after M29's length/length-onto, bootstrap_reverse_demo.asm. First bootstrap demo whose result is a CONS structure rather than a fixnum -- fpga/sim/tb_bootstrap_reverse.sv verifies by walking car_ram/cdr_ram directly (same pattern as tb_bootstrap_pair.sv). PASSED on the first real run (M28/M29's unquoted-test-data lesson applied from the start, no retraction cycle this time). Runner Icarus Verilog 12.0 (devel), simulated-time-units 123292900."))
